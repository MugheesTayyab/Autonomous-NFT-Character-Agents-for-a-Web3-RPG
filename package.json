{
  "name": "autonomous-nft-character-agents",
  "version": "1.0.1",
  "private": true,
  "description": "Autonomous NFT Character Agents for a Web3 RPG — EVM ERC-721 Dynamic Traits + ERC-20 Staking + LangGraph Agent Layer",
  "scripts": {
    "dev": "npm run dev --prefix dashboard",
    "build": "node -e \"const fs=require('fs'),cp=require('child_process'); if(fs.existsSync('dashboard/package.json')){ cp.execSync('cd dashboard && npm install && npm run build', {stdio:'inherit'}); } else { cp.execSync('npm run build', {stdio:'inherit'}); }\"",
    "preview": "npm run preview --prefix dashboard"
  },
  "workspaces": [
    "dashboard"
  ],
  "optionalDependencies": {
    "@esbuild/linux-x64": "^0.21.5",
    "@rollup/rollup-linux-x64-gnu": "^4.18.0",
    "@rollup/rollup-linux-x64-musl": "^4.18.0"
  },
  "keywords": [
    "autonomous-agents",
    "web3",
    "nft",
    "erc721",
    "erc20",
    "langgraph",
    "polygon",
    "solidity",
    "hardhat"
  ],
  "author": "Mughees Tayyab",
  "license": "MIT"
}
